{
  "name": "proyecto",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "clean": "rimraf dist .cache .cache-loader",
    "dev": "npm-run-all clean start",
    "buildClear": "npm-run-all clean build",
    "compile:sass": "node-sass src/sass/main.scss src/estilos_web/estilos.css --watch"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "node-sass": "^8.0.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
